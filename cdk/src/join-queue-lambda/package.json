{
  "name": "join-queue-lambda",
  "version": "1.0.0",
  "description": "Lambda function to join a queue",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@types/aws-lambda": "^8.10.0",
    "aws-lambda": "^1.0.7",
    "typescript": "^4.0.0"
  }
}
