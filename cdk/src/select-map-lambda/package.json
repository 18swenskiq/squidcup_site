{
  "name": "select-map-lambda",
  "version": "1.0.0",
  "description": "Lambda function for selecting maps in lobbies",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@aws-sdk/client-ssm": "^3.0.0",
    "@types/aws-lambda": "^8.10.143",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {}
}
