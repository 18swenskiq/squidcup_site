{
  "name": "get-queue-history-lambda",
  "version": "1.0.0",
  "description": "Lambda function to get user's queue history",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist && rm -rf *.js && rm -rf *.d.ts"
  },
  "dependencies": {
    "@squidcup/shared-lambda-utils": "file:../../../packages/shared-lambda-utils",
    "@squidcup/types": "file:../../../packages/types-squidcup"
  },
  "devDependencies": {
    "@aws-sdk/client-lambda": "^3.693.0",
    "@types/aws-lambda": "^8.10.145",
    "typescript": "^5.7.2"
  }
}
