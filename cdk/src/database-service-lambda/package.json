{
  "name": "database-service-lambda",
  "version": "1.0.0",
  "description": "Centralized database service for MySQL operations",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mysql2": "^3.6.0"
  },
  "devDependencies": {
    "@aws-sdk/client-ssm": "^3.0.0",
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^18.0.0",
    "typescript": "^4.9.0"
  }
}
