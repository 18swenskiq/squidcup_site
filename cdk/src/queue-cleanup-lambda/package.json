{
  "name": "queue-cleanup-lambda",
  "version": "1.0.0",
  "description": "Lambda function to cleanup expired queues and store timeout events",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "typescript": "^4.0.0"
  }
}
