{
  "name": "get-servers-lambda",
  "version": "1.0.0",
  "description": "Lambda function to retrieve servers data",
  "main": "index.js",
  "types": "index.ts",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.759.0",
    "@aws-sdk/lib-dynamodb": "^3.759.0"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
